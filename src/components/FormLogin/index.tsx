

export default function FormLogin(){

    const handleLogin = ()=>{


    }

    return(
        <form onSubmit={handleLogin}>
            {/* Campos de Login */}


            <button type="submit"> ENTRAR</button>
        </form>
    )
}